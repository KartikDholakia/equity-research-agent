[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: pure Python tests, no external dependencies
    integration: tests that mock external APIs (HTTP, Anthropic SDK)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
